[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.backends._legacy:_Backend"

[project]
name = "cskit"
version = "0.1.0"
description = "Codex / CC Switch 运维工具集"
requires-python = ">=3.9"
license = {text = "MIT"}
